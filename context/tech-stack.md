# Tech Stack – klowi-mcf-assistant

> Auto-generated by Claude Code on 2026-04-29 (first session). Update when stack evolves.

## Runtime

- **Node.js** 22.x (local dev: 22.22.0)
- **npm** 10.9.x — package manager (no pnpm/yarn lockfile)

## Application

| Layer | Choice | Version |
|---|---|---|
| Framework | Next.js (App Router, Turbopack) | 16.2.4 |
| UI runtime | React | 19.2.4 |
| Language | TypeScript (strict) | 5.x |
| Styling | Tailwind CSS v4 (PostCSS plugin) | 4.x |
| Linting | ESLint flat config + `eslint-config-next` | 9.x / 16.2.4 |
| Fonts | `next/font` — Geist Sans + Geist Mono | bundled |

## Model layer

- **Anthropic SDK** (`@anthropic-ai/sdk` ^0.91)
- Default model: **`claude-sonnet-4-6`** (`lib/anthropic.ts`)
- Server-only client; no exposure to browser bundle (`server-only` import guard)
- Prompt caching: planned, not yet wired (Phase 2)

## Persistence

- **Vercel Postgres** (Neon under the hood) via `@vercel/postgres` ^0.10
- ORM: **Drizzle ORM** ^0.45 (`drizzle-orm/vercel-postgres`)
- Migrations: `drizzle-kit` ^0.31, config at `drizzle.config.ts`, output `./drizzle`
- Schema: `lib/db/schema.ts` (chats, messages)

## Other libs

- `react-markdown` ^10 + `remark-gfm` ^4 — markdown rendering for coach output
- `clsx` ^2 + `tailwind-merge` ^3 — class composition (`lib/utils.ts:cn`)
- `server-only` ^0 — server-side import guard

## Directory layout

```
.
├── app/                       Next.js App Router pages
│   ├── layout.tsx             Root layout (lang="fr", Geist fonts)
│   ├── page.tsx               Landing
│   └── globals.css            Tailwind + theme tokens
├── lib/
│   ├── anthropic.ts           Anthropic client + model id
│   ├── system-prompt.ts       System prompt assembly (server-side)
│   ├── utils.ts               cn() helper
│   └── db/
│       ├── client.ts          Drizzle client (Vercel Postgres)
│       └── schema.ts          chats + messages tables
├── context/                   PO-facing project state (Nexus convention)
│   ├── prompt/                System prompt fragments (committed)
│   │   ├── README.md
│   │   └── 00-identity.md     Bootstrap-naming flow
│   └── references/
│       └── CONTEXT-COACH.md   Original coach briefing
├── public/                    Static assets (cleaned of scaffold defaults)
├── mcf/                       SYMLINK → Google Drive (private, gitignored)
├── drizzle.config.ts
├── next.config.ts
├── tsconfig.json
├── eslint.config.mjs
├── postcss.config.mjs
├── package.json
├── AGENTS.md                  Next.js 16 hints for AI assistants
├── CLAUDE.md                  Workflow + project rules for Claude Code
└── DEVS.md                    Workflow for human contributors
```

## Conventions

- **Strict TypeScript**, `noEmit`, `moduleResolution: "bundler"`.
- **Server components** by default; client components only where interactivity is needed (chat input, message list).
- Route handlers (`app/api/*/route.ts`) for streaming Anthropic calls.
- Path alias `@/*` → repo root.
- Env in `.env.local` (gitignored); `.env.local.example` lists required vars.
- French in user-facing copy (`lang="fr"`); English in code, types, and English-side prompts when triggered.

## NPM scripts

```bash
npm run dev      # next dev (Turbopack)
npm run build    # next build (Turbopack)
npm run start    # next start
npm run lint     # eslint
```

## Known audit findings (dev-only, not exploitable)

- `esbuild <=0.24.2` — transitive via `drizzle-kit` (`@esbuild-kit/*`). Dev-only.
- `postcss <8.5.10` — transitive via Next.js. Pinned by Next 16; will lift on Next minor bump.

Both are moderate severity advisories on dev-time tooling, no shipped impact.

## Not yet wired (Phase 2+)

- `app/api/chat/route.ts` (streaming endpoint)
- Drizzle schema → migration → Postgres connection
- System prompt assembly call site (caching breakpoints)
- shadcn/ui (will be initialized when first primitive is needed)
- Vercel Password Protection (set on the Vercel project, not in code)
- Anthropic web_search tool wiring
